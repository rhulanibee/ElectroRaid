import { ResidentShell } from "@/components/resident-shell";
import { ResidentTrack } from "@/components/resident-track";

export default function ResidentTrackPage() {
  return (
    <ResidentShell>
      <ResidentTrack />
    </ResidentShell>
  );
}
